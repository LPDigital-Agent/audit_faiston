# =============================================================================
# Test Dependencies for AgentCore Inventory Service
# =============================================================================
# These dependencies are ONLY for testing and should NOT be deployed.
# Install with: pip install -r requirements-test.txt
# =============================================================================

# Include production dependencies (tests must import production code)
-r requirements.txt

# Testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-asyncio>=0.23.0  # Async test support for ResultValidationHook tests

# Code quality
ruff>=0.1.0
mypy>=1.7.0

# Test utilities
freezegun>=1.4.0  # Mock datetime
faker>=20.0.0     # Generate test data

# Security analysis tools (MANDATORY for audit-static target)
bandit>=1.7.5     # SAST for Python
safety>=2.3.0     # Dependency vulnerability scanner
pip-audit>=2.6.0  # Audit pip packages for CVEs
